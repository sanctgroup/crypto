[package]
name = "sanct-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
sanct-crypto = { path = "../sanct-crypto", default-features = false, features = ["wasm"] }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
